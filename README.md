# 💬 Group Chat with WebSockets

Современное веб-приложение для группового чата в реальном времени, построенное на FastAPI и WebSockets. Поддерживает множественные комнаты, мгновенную доставку сообщений и красивый пользовательский интерфейс.

## ✨ Особенности

- 🚀 **Реальное время**: Мгновенная доставка сообщений через WebSocket соединения
- 🏠 **Множественные комнаты**: Поддержка нескольких чат-комнат одновременно
- 🎨 **Современный UI**: Красивый интерфейс на основе Tailwind CSS
- 👥 **Уведомления**: Автоматические уведомления о присоединении/выходе пользователей
- 📱 **Адаптивный дизайн**: Работает на всех устройствах
- 🔄 **Автообновление**: Автоматическая перезагрузка при разработке (reload mode)

## 🛠 Технологии

- **Backend**: 
  - [FastAPI](https://fastapi.tiangolo.com/) - современный веб-фреймворк для Python
  - [Uvicorn](https://www.uvicorn.org/) - ASGI сервер
  - [WebSockets](https://websockets.readthedocs.io/) - для двусторонней связи в реальном времени

- **Frontend**:
  - [Tailwind CSS](https://tailwindcss.com/) - утилитарный CSS фреймворк
  - Vanilla JavaScript - нативный JS для WebSocket клиента
  - Jinja2 - шаблонизатор для HTML

## 📋 Требования

- Python 3.8+
- pip (менеджер пакетов Python)

## 🚀 Установка

1. **Клонируйте репозиторий** (или скачайте проект):
   ```bash
   git clone <repository-url>
   cd Group-chat-with-WebSockets
   ```

2. **Создайте виртуальное окружение**:
   ```bash
   python -m venv venv
   ```

3. **Активируйте виртуальное окружение**:
   
   **Windows:**
   ```bash
   venv\Scripts\activate
   ```
   
   **Linux/Mac:**
   ```bash
   source venv/bin/activate
   ```

4. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

## 🎯 Запуск приложения

1. **Убедитесь, что виртуальное окружение активировано**

2. **Запустите сервер**:
   ```bash
   python app/main.py
   ```

   Или используйте uvicorn напрямую:
   ```bash
   uvicorn app.main:app --host 0.0.0.0 --port 8005 --reload
   ```

3. **Откройте браузер** и перейдите по адресу:
   ```
   http://localhost:8005
   ```

## 📖 Использование

### Вход в чат

1. На главной странице введите:
   - **Ваше имя** (например: "Иван")
   - **ID комнаты** (например: 1, 2, 3...)

2. Нажмите кнопку **"Войти в чат"**

### Отправка сообщений

- Введите текст в поле ввода
- Нажмите кнопку **"Отправить"** или клавишу **Enter**
- Ваши сообщения отображаются справа (синие), сообщения других пользователей - слева (серые)

### Создание комнат

- Пользователи с одинаковым **ID комнаты** попадают в одну комнату
- Можно создать несколько комнат, используя разные ID (1, 2, 3 и т.д.)
- Откройте несколько вкладок браузера с разными именами и одним ID комнаты для тестирования

## 📁 Структура проекта

```
Group-chat-with-WebSockets/
│
├── app/
│   ├── __init__.py
│   ├── main.py                 # Главный файл приложения
│   │
│   ├── api/
│   │   ├── router_page.py      # Роутер для HTML страниц
│   │   └── router_socket.py    # Роутер для WebSocket соединений
│   │
│   ├── static/
│   │   └── index.js            # JavaScript клиент для WebSocket
│   │
│   └── templates/
│       ├── home.html           # Страница входа в чат
│       └── index.html         # Страница чата
│
├── requirements.txt            # Зависимости проекта
└── README.md                   # Документация
```

## 🔧 Конфигурация

### Изменение порта

По умолчанию сервер запускается на порту **8005**. Чтобы изменить порт, отредактируйте файл `app/main.py`:

```python
run("main:app", host="0.0.0.0", port=8005, reload=True)
```

Замените `8005` на нужный вам порт. Также не забудьте обновить URL в `app/static/index.js`:

```javascript
const ws = new WebSocket(`ws://localhost:8005/ws/chat/${roomId}/${userId}?username=${username}`);
```

## 🐛 Решение проблем

### Сообщения не отображаются

1. **Проверьте консоль браузера** (F12) на наличие ошибок
2. **Убедитесь, что сервер запущен** и работает на порту 8005
3. **Очистите кэш браузера** (Ctrl+F5) для загрузки обновленного JavaScript
4. **Проверьте WebSocket соединение** в консоли браузера - должно быть сообщение "Соединение установлено"

### Ошибка подключения WebSocket

- Убедитесь, что порт в `index.js` совпадает с портом сервера
- Проверьте, что сервер запущен и доступен
- Убедитесь, что нет конфликтов с другими приложениями на том же порту

### Статус 304 Not Modified

Это нормальное поведение браузера, означающее использование кэшированной версии файла. Для загрузки новой версии:
- Сделайте жесткое обновление страницы: **Ctrl+F5** (Windows) или **Cmd+Shift+R** (Mac)
- Очистите кэш браузера

## 🔐 Безопасность

⚠️ **Важно**: Это учебный проект. Для production использования необходимо добавить:

- Аутентификацию и авторизацию пользователей
- Валидацию и санитизацию входных данных
- Защиту от XSS и CSRF атак
- HTTPS/WSS для безопасного соединения
- Rate limiting для предотвращения злоупотреблений

## 📝 API Endpoints

### HTTP Endpoints

- `GET /` - Главная страница (форма входа)
- `POST /join_chat` - Вход в чат (принимает `username` и `room_id`)

### WebSocket Endpoints

- `WS /ws/chat/{room_id}/{user_id}?username={username}` - Подключение к чату

---

